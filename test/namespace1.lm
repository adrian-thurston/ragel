namespace n1

	lex
		token id / 
			( 'a' .. 'z' | 'A' .. 'Z' | '_' ) .
			( 'a' .. 'z' | 'A' .. 'Z' | '0' .. '9' | '_' ) *
		/

		ignore / 
			( '\n' | '\t' | ' ' ) .
			( '\n' | '\t' | ' ' )* 
		/
	end

	def start
		[id*]
end

parse P: n1::start[stdin]
print( P.tree '\n' )
